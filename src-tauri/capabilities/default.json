{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "The wallet window: core IPC (commands + events the shell emits: service-log, download-progress, service-state) and opening external links in the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
